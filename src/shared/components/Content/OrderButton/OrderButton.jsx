import React from 'react';

const OrderButton = () => {
    return (
        <div className="orderButton">
            <button>Отправить</button>
        </div>
    );
};

export default OrderButton;
